# at-flight-authorization

## Payload Examples

See `docs/payload-examples.md` for curl commands and JSON examples that create flight authorization proposals, approvals, and notifications with their nested resources.